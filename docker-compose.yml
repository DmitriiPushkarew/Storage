version: '3.8'
services:
  postgres:
    image: postgres
    container_name: video_creator_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: andante371
      POSTGRES_DB: video_creator
    ports:
      - "5432:5432"
    networks:
      - pgnetwork

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: dpushkarev666@gmail.com
      PGADMIN_DEFAULT_PASSWORD: andante371
    ports:
      - "8082:80"
    networks:
      - pgnetwork

networks:
  pgnetwork:
    driver: bridge